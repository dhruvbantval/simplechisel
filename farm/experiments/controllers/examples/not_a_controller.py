# Not a controller at all — no Controller/PID class, no controller() function.
# Uploading this should come back as an ERROR (can't run this shape), not a fail.
def helper(x):
    return x + 1

MESSAGE = "this file has nothing the farm can drive as a controller"
